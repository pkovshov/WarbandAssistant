env_list = ["doctest"]

[env_run_base]
deps = [
    "pytest",
    "-r requirements.txt",
]
setenv = [
    {PYTHONPATH = "{toxinidir}:{toxinidir}/src"}
]
commands = [
  ["pytest", "--doctest-modules"],
]
